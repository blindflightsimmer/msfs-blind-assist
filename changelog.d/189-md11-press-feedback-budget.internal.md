MD-11 internals: registering a hold-to-test button's press and queuing its release are now one atomic step under the held table's lock, closing a narrower race inside the button's release-on-aircraft-switch guarantee where the two could still land out of order; and a guarded press's confirmation now always speaks inside its own echo window, however far behind the event queue is, instead of risking a late arrival that gets told twice. No spoken sentence changed.
