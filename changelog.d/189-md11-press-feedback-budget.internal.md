MD-11 internals: a hold-to-test button's press and its release are now queued under one lock, so an aircraft switch landing between them can no longer leave the button held; a button with no press event writes nothing at all; and a press confirmation is now bounded to speak inside its own echo window however long the event queue ahead of it is, so it can never be spoken twice. No change to what a pilot hears.
