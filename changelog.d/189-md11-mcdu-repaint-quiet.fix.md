The MD-11 clears its MCDU screen before redrawing it, so every page change briefly publishes an empty screen. The window took that literally: it announced "MCDU is blank" and then re-read the page title about four tenths of a second later, on every single page change. The window now holds the page through a redraw and says nothing, and a screen that is genuinely empty is shown in the display rather than spoken.
