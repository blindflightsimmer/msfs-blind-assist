MD-11 MCDU window: when the app was started with the aircraft already flying, the window opened empty and stayed empty until you pressed a key on the MCDU - the aircraft only publishes its screens when they change, and the app was only listening for changes. It now reads all three units the moment it connects, so the window shows the current page as soon as it opens. If a unit really has delivered nothing, the window says "no data received yet" instead of showing a blank list.
